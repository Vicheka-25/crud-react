import { PageHeader } from "antd";

const Header = () => {
  return (
    <PageHeader
      className="site-page-header"
      title="CRUD app"
      subTitle="With react"
    />
  );
};

export default Header;
